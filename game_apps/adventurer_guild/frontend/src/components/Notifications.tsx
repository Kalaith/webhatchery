import React from 'react';

const Notifications: React.FC = () => {
    return (
        <div className="notification-container" id="notifications">
            {/* Notifications will be added dynamically */}
        </div>
    );
};

export default Notifications;
